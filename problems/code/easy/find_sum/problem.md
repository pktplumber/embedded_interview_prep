### In C, write a function that takes an array of integers and returns the indices of two elements that match the desired number
If there are no numbers in the array that add up to the target, return -1. Otherwise, return 0.

`int find_indices(const int* numbers, const size_t count, int target, int* idx1, int* idx2)`