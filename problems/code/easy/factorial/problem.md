### Create a factorial function in c
The function will take a uint32_t as an argument and it must output the factorial of the number:

`uint64_t find_factorial(const uint32_t val)`

What if the input was signed instead? How would you handle invalid are too large of inputs? 
