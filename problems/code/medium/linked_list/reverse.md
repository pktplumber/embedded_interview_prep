# Implement a function to reverse a singly linked list in place in C

```
struct Node {
    int data;
    struct Node* next;
};

// Function to reverse the linked list
struct Node* reverseList(struct Node* head);
```