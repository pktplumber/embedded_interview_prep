# Swap nodes in linked list

Write a function that swaps two nodes in a singly linked list given the two node indices and the head of the list.

```
struct Node
{
    int value;
    struct Node* next;
};

void swap_nodes(struct Node* head, int node1, int node2);
```
