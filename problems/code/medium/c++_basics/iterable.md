## Recreate the vector class in C++ using a constant capacity
The vector should have:
- A custom/template-based type
- A `push_back` method
- A `pop` method
- Iterators
- An aligned internal container of the underlying type
- `size` and `capacity` methods
