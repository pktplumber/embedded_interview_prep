### Design a finite state machine library.
The library must:
1. Support transitions multiple states defined by the user
2. Transition between states when events occur. The transition (state->state given the event) is defined by the user.
3. Suppot an optional callback/function that is called when a particular transition between states occurs. The specific function must be configurable by the user.

The library can be implemented in any language. 
